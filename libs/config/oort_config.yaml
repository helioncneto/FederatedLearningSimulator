round_penalty: 2.0                    # Penalty factor in our paper (\alpha), \alpha -> 0 turns to (Oort w/o sys)
pacer_delta: 10
pacer_step: 20
cut_off_util: 0.4                     # Determined by W * K
clip_bound: 0.95
blacklist_max_len: 5
blacklist_rounds: 10                  # Number of rounds needed to blacklist
exploration_factor: 0.9
exploration_decay: 0.98
exploration_min: 0.2
sample_window: 20
exploration_alpha: 2
round_threshold: 10
round_prefer_duration: 100
